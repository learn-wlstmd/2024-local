apiVersion: apps/v1
kind: Deployment
metadata:
  name: golang-api-ec2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: golang-api
  template:
    metadata:
      labels:
        app: golang-api
    spec:
      containers:
      - name: golang-api
        image: 362708816803.dkr.ecr.ap-northeast-2.amazonaws.com/golang-api
        ports:
        - containerPort: 8080
      nodeSelector:
        app: golang-api-node
---
apiVersion: v1
kind: Service
metadata:
  name: golang-api-ec2
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: golang-api
